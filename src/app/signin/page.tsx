import { AuthEntryShell, SignInForm } from '@/components/auth';

export default function SignInPage() {
  return (
    <AuthEntryShell title="Sign In">
      <SignInForm />
    </AuthEntryShell>
  );
}
